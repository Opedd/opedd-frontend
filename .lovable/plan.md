

# Plan: Unify Environment to External Supabase Project

## Current State Summary

| Component | Current Value | Target Value | Status |
|-----------|---------------|--------------|--------|
| `src/integrations/supabase/client.ts` | `djdzcciayennqchjgybx` | `djdzcciayennqchjgybx` | Already correct |
| `src/lib/api.ts` | `djdzcciayennqchjgybx` | `djdzcciayennqchjgybx` | Already correct |
| `supabase/config.toml` | `dmmvaiupksamhgefdxev` | Cannot change | Auto-generated |
| `src/integrations/supabase/types.ts` | Lovable Cloud schema | External schema | Auto-generated |

## Key Finding

The frontend code is **already correctly configured** to use your external Supabase project (`djdzcciayennqchjgybx`). Both the Supabase client and API endpoints are hardcoded to this project.

## What I Cannot Change

These files are **auto-generated by Lovable Cloud** and cannot be manually edited:
- `supabase/config.toml` - Will always show the Lovable Cloud project ID
- `src/integrations/supabase/client.ts` - Already points to external project (hardcoded override)
- `src/integrations/supabase/types.ts` - Reflects Lovable Cloud schema, not external

## Schema Compatibility Analysis

Your external schema (with views):
- `assets` (view with INSTEAD OF triggers)
- `rss_sources` (view with INSTEAD OF triggers)

Current auto-generated types show:
- `assets` table with: `publication_id`, `verification_status`, `human_price`, `ai_price`, etc.
- `rss_sources` table with: `feed_url`, `name`, `platform`, `sync_status`, etc.

**Good news**: The column names match! The TypeScript types should work correctly with your external views since they expose the same columns.

## Implementation Steps

### Step 1: Verify Current Connection (No Code Changes)
The hardcoded client in `src/integrations/supabase/client.ts` already overrides any Lovable Cloud configuration:
```typescript
const SUPABASE_URL = 'https://djdzcciayennqchjgybx.supabase.co';
const SUPABASE_ANON_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...';
```

### Step 2: Confirm API Routing (No Code Changes)
`src/lib/api.ts` already routes all edge function calls to the external project:
```typescript
const EDGE_FUNCTION_BASE = 'https://djdzcciayennqchjgybx.supabase.co/functions/v1';
```

### Step 3: Test the Connection
After plan approval, I will:
1. Open the browser tool to navigate to the app
2. Attempt a "Sync" action to verify the connection works
3. Check network requests to confirm they hit `djdzcciayennqchjgybx`
4. Report any errors from the edge function responses

## Technical Explanation

**Why you see two project IDs:**
- **Lovable Cloud** (`dmmvaiupksamhgefdxev`) - The managed backend attached to this Lovable project. It controls `config.toml` and `types.ts`.
- **Your External Project** (`djdzcciayennqchjgybx`) - Your separately managed Supabase project with Claude Code. The frontend code is hardcoded to use this.

**Why this works:**
The Supabase client is created with hardcoded credentials, which override any environment configuration. All database queries and edge function calls go to your external project.

## What "Disconnect" Means

I cannot truly "disconnect" Lovable Cloud because:
1. The `supabase/config.toml` file is managed by Lovable's infrastructure
2. The auto-generated types file reflects Lovable Cloud schema

However, **the frontend code is already completely bypassing Lovable Cloud** by using hardcoded credentials for your external project. The Lovable Cloud database exists but is unused.

## Summary

**No code changes are required.** The configuration is already correct. The implementation phase will focus on:
1. Testing the end-to-end connection
2. Verifying edge function responses from your external project
3. Confirming the schema compatibility works at runtime

